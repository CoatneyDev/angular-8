import { DynamicDataDirective } from './dynamic-data.directive';

describe('DynamicDataDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDataDirective();
    expect(directive).toBeTruthy();
  });
});
